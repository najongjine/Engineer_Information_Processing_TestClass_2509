main 에 있는 a,b 값 교환 안일어남
main에서 swap 함수에 값을 줄때, 값을 ctrl+c, ctrl+v 로 전달함
#include <stdio.h>

void swap(int _a, int _b){
    int temp=_a;
    _a=_b;
    _b=_a;
}
int main() {
    int a=1;
    int b=2;
    swap(a,b);
    printf("a:%d, b:%d \n",a,b)

    return 0;
}


main에 있는 a 와 b가 서로 값을 교환함
a의 주소, b의 주소를 주니깐(본체를 주니깐) 값 교환이 일어남
#include <stdio.h>

void swap(int *_a, int *_b){
    int temp=*_a;
    *_a=*_b;
    *_b=temp;
}
int main() {
    int a=1;
    int b=2;
    swap(&a,&b);
    printf("a:%d, b:%d \n",a,b);

    return 0;
}


int number = 42;

/* pointer 변수에 number의 주소 저장하기 */
int *pointer = &number
int c=1;

/* number 와 c 를 2진수로 변환한후, AND 연산하기 */
int a= number & c;

/* number 변수의 주소를 뽑기 */
printf(“%d\n”, &number);

/* pointer 변수가 가르키는 진짜 값을 보여주기 */
printf(“%d\n”, *pointer);




int num = 42;
char str[] = "Hello";
int arr[] = {1, 2, 3, 4, 5};

/* 배열과 포인터는 결국 비슷한 놈이지만,
차이점은 배열은 주소값을 변경할수 없고, 포인터는 주소값을 변경할수 있다 */
int *ptr = &num;
ptr= arr;

printf("%d\n", &num);
printf("%d\n", *ptr);
printf("%s\n", str);

// arr[0], *ptr 이거는 같다
printf("%d\n", arr[0]);
printf("%d\n", *ptr);

return 0;