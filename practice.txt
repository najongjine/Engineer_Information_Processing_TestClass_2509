#include <stdio.h>
main( ) {
    int a[10];
    a[0] = 0;
    a[1] = 1;
    for (int i = 0; i < 8; i++)
        a[i + 2] = a[i + 1] + a[i];
    printf("%d", a[9]);
}






int x=1, y=6;

while (y--) { // y = 0   조건체크는 하니깐 -- 가 나중에 발동됨
    x++;
}
System.out.println("x=" + x + "y=" + y); // 7 , -1